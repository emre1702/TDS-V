using TDS.Enum;

namespace TDS.Instance.Lobby.Interfaces
{

    interface IRound
    {
        //void StartRoundGame();
        //void StartMapChoose();
        //void EndRoundEarlier(ERoundEndReason reason, object arg = null);
        //void CheckForEnoughAlive();
    }
}
