namespace TDS.server.enums {
    enum LobbyStatus {
        NONE, MAPCHOOSE, COUNTDOWN, ROUND, ROUNDEND
    }

    enum Language {
        ENGLISH, GERMAN
    }

    enum MapType {
        NORMAL, BOMB
    }
}
